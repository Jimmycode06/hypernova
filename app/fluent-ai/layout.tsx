import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Fluent AI — Speak with confidence from day one",
  description: "Practice foreign languages through realistic AI conversations, personalized corrections, and lessons adapted to your level.",
};

export default function Layout({ children }: { children: React.ReactNode }) { return children; }
