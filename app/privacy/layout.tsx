import type { Metadata } from "next";
export const metadata: Metadata = { title: "Privacy Policy — Fluent AI", description: "Learn how Fluent AI handles conversation, voice, subscription, and support information." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
