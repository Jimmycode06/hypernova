import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fluent AI Support", description: "Get help with Fluent AI conversations, voice recognition, subscriptions, purchases, and data requests." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
